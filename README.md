# האתר של תומר בן דוד — tomerbendavid.co.il

אתר סטטי (HTML/CSS/JS בלבד) — אין תלות בשום פלטפורמה. אפשר להעלות אותו לכל שרת אחסון.

## מבנה הקבצים

| קובץ | מה זה |
|---|---|
| `index.html` | דף הבית (משמש גם כדף הנחיתה) — הצגה, "עלי", קישורים, יצירת קשר |
| `individuals.html` | לאנשים ומתרגלים — קבוצות קונג פו + לוח זמנים, אימון אישי, ריטריטים, הכשרת מורים |
| `organizations.html` | לארגונים וצוותים — סדנת יין־יאנג, סדנאות מותאמות, ליווי מנהלים |
| `contact.html` | צור קשר — שני טפסים בלשוניות (אנשים / ארגונים) |
| `css/style.css` | כל העיצוב |
| `js/main.js` | תפריט נייד, אנימציות, "עוד פרטים" |
| `js/contact.js` | לשוניות הטופס ושליחה בוואטסאפ |
| `assets/img/` | כל התמונות (הורדו מהאתר הקיים) + לוגו |

## איך הטופס עובד

אין צורך בשרת: לחיצה על "שליחה בוואטסאפ" פותחת וואטסאפ אל 052-700-0030
עם הודעה מוכנה שמכילה את כל מה שמולא בטופס. יש גם קישור חלופי למייל tomer@orin.co.il.
אם בעתיד רוצים שהטופס ישלח מייל אמיתי מהשרת — אפשר לחבר שירות כמו FormSubmit או סקריפט PHP באחסון.

- קישור ישיר לטופס ארגונים: `contact.html?type=org`
- קישור ישיר לטופס אנשים: `contact.html?type=individual` (או סתם `contact.html`)

## העלאה לאוויר (בסוף העבודה)

1. להעלות את כל תוכן התיקייה הזו (כולל תיקיות `css`, `js`, `assets`) לתיקיית השורש של האחסון (בדרך כלל `public_html` או `www`).
2. לוודא שהדומיין tomerbendavid.co.il מפנה לשרת (רשומת A / NS אצל רשם הדומיין).
3. להפעיל HTTPS (באחסון ישראלי בדרך כלל יש Let's Encrypt בלחיצה).
4. זהו — אין התקנות, אין מסדי נתונים.

## דברים שנשארו פתוחים

- **קישור לאתר לבונה** — עדיין אין אתר; כרגע מופיע "אתר בבנייה" בדף הבית. כשיהיה קישור, לעדכן ב־`index.html` (לחפש "לבונה").
- **תאריך פרסום הכתבה** — לוודא מול תומר שהאתר עולה לפני.
- **גישה לאתר הפגודה** (ha-pagoda.com) — לבקש מתומר, לפי הצורך בהצעה.
- **טקסטים** — תומר כתב שנשארה לו "עוד ממש קצת עבודה" על חלק מהטקסטים; כשישלח נוסח סופי, לעדכן בקבצים.

## עדכון טקסטים

כל הטקסטים נמצאים ישירות בקובצי ה־HTML — פותחים בכל עורך טקסט, מחפשים את המשפט ומחליפים.
מספר הטלפון והמייל מופיעים בכל הדפים (פוטר + כפתור וואטסאפ צף) — אם משתנים, לחפש
`972527000030` ו־`tomer@orin.co.il` ולהחליף בכל הקבצים.
